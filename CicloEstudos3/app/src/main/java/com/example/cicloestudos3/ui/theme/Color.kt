package com.example.cicloestudos3.ui.theme

import androidx.compose.ui.graphics.Color

// ── Estudos (Shared Design System — Purple) ────────────────────────────────────
val EstudosPrimary    = Color(0xFF8B5CF6)   // accent
val EstudosDark       = Color(0xFF4C1D95)   // dark accent (status bar, gradient end)
val EstudosBackground = Color(0xFFFAFAFF)   // app background
val EstudosMuted      = Color(0xFF94A3B8)   // muted gray (inactive nav, secondary text)
val EstudosTitle      = Color(0xFF2E1065)   // titles
val EstudosDanger     = Color(0xFFEF4444)   // danger / delete

// Supporting tints
val EstudosSurface       = Color(0xFFFFFFFF)   // white cards
val EstudosPrimarySoft   = Color(0xFFF5F3FF)   // light purple chip / icon bg
val EstudosPrimaryDeep   = Color(0xFF7C3AED)   // pill text
val EstudosEyebrow       = Color(0xFFC4B5FD)   // eyebrow label on header
val EstudosTrack         = Color(0xFFF1F5F9)   // progress track / neutral chip bg
val EstudosLabel         = Color(0xFF374151)   // form label
val EstudosBorder        = Color(0xFFE5E7EB)   // input border
val EstudosDangerSoft    = Color(0xFFFEF2F2)   // danger soft bg
val EstudosDone          = Color(0xFF059669)   // green (revision done)
val EstudosDoneSoft      = Color(0xFFECFDF5)   // green soft bg
val EstudosChevron       = Color(0xFFCBD5E1)   // chevron / disabled

/** Discipline color palette (from prototype DISC_COLORS) */
val subjectColorPalette = listOf(
    "#DC2626", // Red
    "#2563EB", // Blue
    "#059669", // Green
    "#D97706", // Amber
    "#7C3AED", // Violet
    "#0891B2", // Cyan
)
