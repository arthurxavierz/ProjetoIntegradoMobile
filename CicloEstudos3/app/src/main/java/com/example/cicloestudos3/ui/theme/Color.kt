package com.example.cicloestudos3.ui.theme

import androidx.compose.ui.graphics.Color

// ── Forest Emerald Palette ────────────────────────────────────────────────────
val ForestDeep        = Color(0xFF052E16)   // darkest — hero gradient start
val ForestMid         = Color(0xFF14532D)   // mid dark
val EmeraldPrimary    = Color(0xFF16A34A)   // vibrant green
val EmeraldLight      = Color(0xFF4ADE80)   // bright accent
val EmeraldContainer  = Color(0xFFDCFCE7)   // light green surface

// ── Accents ───────────────────────────────────────────────────────────────────
val AmberStreak       = Color(0xFFF59E0B)   // warm amber — streak, highlights
val AmberContainer    = Color(0xFFFEF3C7)
val TealSecondary     = Color(0xFF0D9488)   // teal secondary
val TealContainer     = Color(0xFFCCFBF1)
val RedAlert          = Color(0xFFDC2626)
val RedContainer      = Color(0xFFFEE2E2)

// ── Surfaces / Backgrounds ────────────────────────────────────────────────────
val BackgroundLight   = Color(0xFFF0FDF4)   // very light green tint
val SurfaceWhite      = Color(0xFFFFFFFF)
val SurfaceVariant    = Color(0xFFE8F5E9)
val OutlineColor      = Color(0xFFBBF7D0)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary       = Color(0xFF052E16)
val TextSecondary     = Color(0xFF4B5563)
val TextOnDark        = Color(0xFFFFFFFF)
val TextOnDarkSub     = Color(0xFFBBF7D0)

// ── Dark Theme ────────────────────────────────────────────────────────────────
val ForestDarkBg      = Color(0xFF0A1A10)
val ForestDarkSurface = Color(0xFF0F2618)
val EmeraldDark       = Color(0xFF4ADE80)
val EmeraldDarkContainer = Color(0xFF14532D)
